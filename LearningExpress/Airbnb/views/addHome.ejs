<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head'); %>
  <body class="bg-gray-50 min-h-screen font-sans">
    <!-- Header -->
    <%- include('partials/header'); %>

    <!-- Heading (outside header bar) -->
    <section class="text-center mt-10">
      <h1 class="text-3xl font-bold tracking-wide text-gray-800">
        Register Your Home 🏡
      </h1>
    </section>

    <!-- Main Section -->
    <main
      class="flex flex-col items-center justify-center flex-grow mt-6 w-full"
    >
      <form
        action="/host/add-home"
        method="POST"
        class="bg-white shadow-lg rounded-2xl p-8 w-80 flex flex-col gap-5 transition-transform hover:scale-105 hover:shadow-xl"
      >
        <h2 class="text-lg font-semibold text-gray-800 text-center">
          Fill in the details below
        </h2>

        <!-- Input Field -->
        <input
          type="text"
          name="houseName"
          placeholder="Enter the name of your house"
          class="border border-gray-300 rounded-lg p-2 focus:outline-none focus:ring-2 focus:ring-red-400"
          required
        />

        <!-- Submit Button -->
        <input
          type="submit"
          value="Submit"
          class="bg-red-500 text-white font-medium py-2 rounded-lg cursor-pointer hover:bg-red-600 transition-colors duration-300"
        />
      </form>
    </main>

    <!-- Footer -->
    <footer class="text-gray-500 text-sm mt-10 mb-4 text-center">
      © 2025 Airbnb Clone | Host your space with confidence ❤️
    </footer>
  </body>
</html>
